<html>
    <head>
        <title>fizzy</title>
        <style>
            body {
                font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
                text-align: center;
            }

            #break {
                color:#efefef;
            }

            #files {
                width: 100%;
                text-align: center;
            }

            #files tr:nth-child(even) {
                background-color: #efefef;
            }

            #files th {
                background-color: #efefef;
            }

            #upload-count {
                font-family: monospace;
            }
        </style>
    </head>
    <body>
        <h1>fizzy</h1>
        <p>fizzy is a stupidly simple file hosting platform. emphasis on stupidly!</p>
        <hr id="break" />
        <form action="/upload" method="POST" enctype="multipart/form-data">
            <input type="file" name="file" multiple />
            <button type="submit">Upload</button>
        </form>
        <div id="upload-count"><b>{{uploadCount}}</b> files have been uploaded!</div>
        <table id="files">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Size</th>
                    <th>Date Uploaded</th>
                    <th>Download</th>
                </tr>
            </thead>
            <tbody>
                {{#each uploads}}
                    <tr>
                        <td>{{this.Filename}}</td>
                        <td>{{this.Size}} bytes</td>
                        <td>{{this.DateUploaded}}</td>
                        <td><a href="/static/{{this.Id}}" download="{{this.Filename}}">Download</a></td>
                    </tr>
                {{/each}}
            </tbody>
        </table>
    </body>
</html>